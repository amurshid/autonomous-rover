<!--
  Nav2's navigate_to_pose_w_replanning_and_recovery, with one change.

  BackUp's default speed is far below this rover's motor deadband. The bridge
  reports deadband=0.05/0.18, so anything at or under 0.05 m/s commands a
  motion the wheels never make. Every backup recovery then ran its full
  timeout while the rover sat still, and Nav2 reported the recovery as
  failed, which looked like a navigation fault and was really a hardware
  floor. 0.15 m/s clears the deadband and is still slow enough indoors.

  Referenced by nav2_params.yaml as default_nav_to_pose_bt_xml, deployed to
  /home/amurshid/nav_to_pose_bt.xml. bt_navigator refuses to activate without
  it, and the lifecycle manager then aborts the whole Nav2 bringup, so a
  missing or malformed file here takes navigation down entirely rather than
  degrading it.

  Note for editors: XML forbids a double hyphen inside a comment. Writing one
  here makes this file unparseable, which fails identically to deleting it.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context"
                                service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{path}" controller_id="FollowPath"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context"
                              service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <Sequence name="ClearingActions">
            <ClearEntireCostmap name="ClearLocalCostmap-Subtree"
                                service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Subtree"
                                service_name="global_costmap/clear_entirely_global_costmap"/>
          </Sequence>
          <Spin spin_dist="1.57"/>
          <Wait wait_duration="5"/>
          <BackUp backup_dist="0.30" backup_speed="0.15"/>
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
